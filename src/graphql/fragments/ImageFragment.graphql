fragment Image on Asset {
  thumbnail: url(
    transformation: { image: { resize: { height: 300, fit: scale } } }
  )
  url(transformation: { image: { resize: { height: 600, fit: scale } } })
  height
  width
  alt
}
